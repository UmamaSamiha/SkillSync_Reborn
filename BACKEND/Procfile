web: FLASK_APP=run.py bash -c 'n=0; until flask db upgrade; do n=$((n+1)); [ $n -ge 5 ] && exit 1; echo "flask db upgrade failed, retry $n/5 in 5s..."; sleep 5; done' && flask seed && gunicorn -c gunicorn.conf.py run:app
